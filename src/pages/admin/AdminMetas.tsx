import ProjecaoMetas from '../ProjecaoMetas';

/** Balão Metas do Administrador — mesma lógica da Projeção de Metas (dashboard de vendas). */
export default function AdminMetas() {
  return <ProjecaoMetas adminMode />;
}
